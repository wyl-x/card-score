# 🚀 快速入门指南

## ⚡ 5分钟快速开始

### 前置要求

**必需**:
- ✅ Java 17 或更高版本 - [下载](https://www.oracle.com/java/technologies/downloads/)
- ✅ Maven 3.6+ - [下载](https://maven.apache.org/download.cgi)
- ✅ Flutter 3.0+ - [下载](https://flutter.dev/docs/get-started/install)

**检查安装**:
```bash
java -version
mvn -version
flutter --version
```

---

## 📦 方法一：使用启动脚本（推荐）

### Windows

1. **启动服务端**
   ```bash
   start-server.bat
   ```
   首次运行会自动构建，等待看到 "Card Score Server Started" 提示

2. **启动客户端**（新开一个终端）
   ```bash
   start-client.bat
   ```
   自动安装依赖并启动应用

### Linux/Mac

1. **启动服务端**
   ```bash
   chmod +x start-server.sh
   ./start-server.sh
   ```

2. **启动客户端**（新开一个终端）
   ```bash
   chmod +x start-client.sh
   ./start-client.sh
   ```

---

## 🔧 方法二：手动启动

### 1️⃣ 构建并启动服务端

```bash
cd server
mvn clean package
java -jar target/card-score-server-1.0.0-jar-with-dependencies.jar
```

**验证服务端**: 访问 http://localhost:8080/health

### 2️⃣ 运行客户端

```bash
cd client
flutter pub get
flutter pub run build_runner build
flutter run
```

---

## 📱 首次使用

### 1. 登录页面
- **服务器地址**: 
  - 本地测试: `http://localhost:8080`
  - 手机连电脑: `http://你的电脑IP:8080` (例如: `http://192.168.1.100:8080`)
- **用户名**: 输入任意名称（如：张三）
- 点击 **登录/注册**

### 2. 创建房间
- 点击右下角 **+** 按钮
- 输入房间名称（如：今晚打牌）
- 点击 **创建**

### 3. 开始转账
- 点击进入房间
- 其他玩家也加入同一个房间
- 点击其他玩家的 **转账** 按钮
- 选择金额或输入自定义金额
- 确认转账，系统自动语音播报

---

## 🌐 多设备使用

### 场景：手机连接电脑上的服务器

1. **确保在同一WiFi**
   - 电脑和手机必须连接同一个WiFi网络

2. **查找电脑IP地址**
   - Windows: 在CMD运行 `ipconfig`，找到 IPv4 地址
   - Mac: 在终端运行 `ifconfig | grep inet`
   - Linux: 在终端运行 `ip addr show`
   
   示例IP: `192.168.1.100`

3. **在手机客户端设置服务器地址**
   - 输入: `http://192.168.1.100:8080`

4. **测试连接**
   - 在手机浏览器访问: `http://192.168.1.100:8080/health`
   - 如果显示JSON响应，说明连接成功

---

## ❓ 常见问题

### 服务端启动失败

**问题**: `Exception in thread "main" java.lang.UnsupportedClassVersionError`
**解决**: Java版本太低，请安装Java 17或更高版本

**问题**: `Address already in use`
**解决**: 端口8080被占用，使用其他端口启动：
```bash
java -jar target/card-score-server-1.0.0-jar-with-dependencies.jar 9090
```

### 客户端连接失败

**问题**: `Failed to create user: Connection refused`
**解决**:
1. 确保服务端已启动
2. 检查服务器地址是否正确
3. 防火墙是否阻止了8080端口

**问题**: `SocketException: Network is unreachable`
**解决**: 
- 检查手机和电脑是否在同一WiFi
- 检查电脑防火墙设置
- Windows: 允许Java通过防火墙

### Flutter相关

**问题**: `Waiting for another flutter command to release the startup lock`
**解决**:
```bash
rm -rf ~/.flutter/bin/cache/lockfile  # Mac/Linux
del %LOCALAPPDATA%\flutter\.flutter_lock  # Windows
```

**问题**: `No devices found`
**解决**:
- Android: 打开模拟器或连接真机，开启USB调试
- iOS: 需要Mac电脑和Xcode

---

## 🎯 快速测试流程

1. ✅ 启动服务端
2. ✅ 浏览器访问 http://localhost:8080/health
3. ✅ 启动客户端
4. ✅ 创建用户A（张三）
5. ✅ 创建房间（今晚打牌）
6. ✅ 创建用户B（李四）并加入房间
7. ✅ 张三向李四转账10分
8. ✅ 听到语音播报
9. ✅ 查看转账记录和总分

---

## 📚 下一步

- 详细文档: [BUILD_AND_RUN.md](BUILD_AND_RUN.md)
- 配置说明: [CONFIG.md](CONFIG.md)
- 项目总览: [PROJECT_SUMMARY.md](PROJECT_SUMMARY.md)
- 完整功能: [Readme.md](Readme.md)

---

## 💡 提示

- 🔊 确保设备音量开启以听到语音播报
- 🔄 房间详情页每5秒自动刷新
- 💾 所有数据保存在服务端 `data/` 目录
- 🚪 退出房间后可随时重新加入，数据不会丢失

---

**需要帮助？** 查看 [BUILD_AND_RUN.md](BUILD_AND_RUN.md) 获取详细故障排除指南
